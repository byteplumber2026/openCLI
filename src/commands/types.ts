export interface CustomCommand {
  name: string;
  description: string;
  prompt: string;
  source: string;
}

export interface CustomCommandDefinition {
  name: string;
  description: string;
  prompt: string;
}
